import type { WatchStatus } from '../types';

export const WATCH_STATUS_ORDER: WatchStatus[] = ['not_watched', 'watching', 'completed'];

export const DEFAULT_PAGE_SIZE = 20;

export const MEDIA_RECOMMENDATION_LIMIT = 10;
